#machine:
# For Python 3.6
#  pre:
#    - cd /opt/circleci/.pyenv; git pull

# Should be already pre-installed
#python:
#  version: 3.5

# Should be done automatically
#dependencies:
#  pre:
#    - "pip install -r requirements.txt"


test:
  override:
    - "python src/examples.py"
