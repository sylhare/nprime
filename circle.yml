machine:
# For Python 3.6
#  pre:
#    - cd /opt/circleci/.pyenv; git pull

python:
  version: 3.5.0
#  version: 3.6.0

dependencies:
  override:
    - "pip install -r requirements.txt"

test:
  override:
    - "python src/examples.py"
