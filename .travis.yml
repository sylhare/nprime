language: python

sudo: false

#notifications:
#   email: false
    
python:
  - "3.5"
  
before_install:
- "pip install -U pip"
- "export PYTHONPATH=$PYTHONPATH:$(pwd)"

# command to install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script: 
  - "python test/test_pyprime.py"
  
#branches:
#  only:
#    - master
    
after_success:
  - echo "Success!"